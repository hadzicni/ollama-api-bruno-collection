info:
  name: Chat (Vision)
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/api/chat"
  body:
    type: json
    data: |-
      {
        "model": "{{visionModel}}",
        "messages": [
          {
            "role": "user",
            "content": "What can you see in this image? What mood does it convey?",
            "images": ["{{imageBase64}}"]
          }
        ],
        "stream": false
      }
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: |-
        // Downloads the sample image ({{imageUrl}}) once and stores it Base64-encoded
        // in the runtime variable "imageBase64". To use your own image, change imageUrl
        // in the environment - the image is re-downloaded automatically.
        const cachedFor = bru.getVar("imageBase64Source");
        const imageUrl = bru.interpolate("{{imageUrl}}");

        if (!bru.getVar("imageBase64") || cachedFor !== imageUrl) {
          const axios = require("axios");
          const response = await axios.get(imageUrl, {
            responseType: "arraybuffer",
            headers: { "User-Agent": "Bruno-Ollama-Collection/1.0" }
          });
          bru.setVar("imageBase64", Buffer.from(response.data).toString("base64"));
          bru.setVar("imageBase64Source", imageUrl);
          console.log("Image loaded:", imageUrl);
        }
    - type: tests
      code: |-
        test("Status 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

        test("Assistant message present", function () {
          const msg = res.getBody().message;
          expect(msg.role).to.equal("assistant");
          expect(msg.content).to.be.a("string");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: |-
  # Chat (Vision)

  Images are sent per message in the `images` field (Base64).
  The pre-request script loads `{{imageUrl}}` automatically.
