info:
  name: 8. Verify Deleted
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/tags"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

        test("Test model was removed", function () {
          const names = res.getBody().models.map(m => m.name);
          expect(names.some(n => n.startsWith("bruno-lifecycle-test"))).to.equal(false);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
