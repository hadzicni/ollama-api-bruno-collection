info:
  name: Model Lifecycle
  type: folder
  seq: 3

request:
  auth: inherit

docs:
  content: |-
    # Model Lifecycle

    End-to-end test of model management using a tiny model (`{{smallModel}}`, ~270 MB):

    Pull -> Show -> Create (custom model) -> Generate -> PS -> Unload -> Delete

    Ideal as a **smoke test** for a fresh Ollama installation.
  type: text/markdown
