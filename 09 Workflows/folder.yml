info:
  name: Workflows
  type: folder
  seq: 9

request:
  auth: inherit

docs:
  content: |-
    # Workflows

    Chained flows - best run with the **Collection Runner** (right-click a subfolder > *Run*).
    The requests share data via runtime variables.
  type: text/markdown
