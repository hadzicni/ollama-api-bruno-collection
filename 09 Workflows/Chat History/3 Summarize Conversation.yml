info:
  name: 3. Summarize Conversation
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/chat"
  body:
    type: json
    data: |-
      {
        "model": "{{model}}",
        "messages": [],
        "stream": false,
        "format": {
          "type": "object",
          "properties": {
            "topic": {
              "type": "string"
            },
            "key_facts": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "required": [
            "topic",
            "key_facts"
          ]
        }
      }
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: |-
        const history = JSON.parse(bru.getVar("chatHistory") || "[]");
        const transcript = history
          .filter(m => m.role !== "system")
          .map(m => (m.role === "user" ? "Student" : "Teacher") + ": " + m.content)
          .join("\n\n");

        const body = req.getBody();
        body.messages = [
          { role: "system", content: "Summarize conversations in a structured way." },
          { role: "user", content: "Summarize this conversation:\n\n" + transcript }
        ];
        req.setBody(body);
    - type: after-response
      code: |-
        try {
          const summary = JSON.parse(res.getBody().message.content);
          console.log("Topic: " + summary.topic);
          summary.key_facts.forEach(f => console.log("   - " + f));
        } catch (e) {}
    - type: tests
      code: |-
        test("Status 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

        test("Structured summary", function () {
          const s = JSON.parse(res.getBody().message.content);
          expect(s.topic).to.be.a("string");
          expect(s.key_facts).to.be.an("array").and.not.be.empty;
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
