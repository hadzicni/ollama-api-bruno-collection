info:
  name: 1. Start Conversation
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/chat"
  body:
    type: json
    data: |-
      {
        "model": "{{model}}",
        "messages": [],
        "stream": false
      }
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: |-
        const history = [
          { role: "system", content: "You are a patient astronomy teacher. Keep answers short (max. 4 sentences)." },
          { role: "user", content: "Why is Mars red?" }
        ];
        bru.setVar("chatHistory", JSON.stringify(history));

        const body = req.getBody();
        body.messages = history;
        req.setBody(body);
    - type: after-response
      code: |-
        const msg = res.getBody()?.message;
        if (msg) {
          const history = JSON.parse(bru.getVar("chatHistory"));
          history.push({ role: "assistant", content: msg.content });
          bru.setVar("chatHistory", JSON.stringify(history));
          console.log("Assistant: " + msg.content);
        }
    - type: tests
      code: |-
        test("Status 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

        test("Assistant message present", function () {
          const msg = res.getBody().message;
          expect(msg.role).to.equal("assistant");
          expect(msg.content).to.be.a("string");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
