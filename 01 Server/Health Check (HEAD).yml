info:
  name: Health Check (HEAD)
  type: http
  seq: 2

http:
  method: HEAD
  url: "{{baseUrl}}/"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: |-
  # Health Check (HEAD)

  `HEAD /` - Same as the health check, but without a response body. Only the status code matters.
