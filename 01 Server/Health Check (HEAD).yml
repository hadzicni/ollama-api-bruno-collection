info:
  name: Health Check (HEAD)
  type: http
  seq: 2

http:
  method: HEAD
  url: "{{baseUrl}}/"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: |-
  # Health Check (HEAD)

  `HEAD /` – Wie der Health Check, aber ohne Response-Body. Nur der Statuscode zählt.
