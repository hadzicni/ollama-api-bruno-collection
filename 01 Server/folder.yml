info:
  name: Server
  type: folder
  seq: 1

request:
  auth: inherit

docs:
  content: |-
    # Server

    Basic endpoints to check whether the Ollama server is running and which version is installed.
  type: text/markdown
