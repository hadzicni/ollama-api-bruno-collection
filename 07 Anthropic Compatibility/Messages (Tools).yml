info:
  name: Messages (Tools)
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/v1/messages"
  body:
    type: json
    data: |-
      {
        "model": "{{toolModel}}",
        "max_tokens": 1024,
        "tools": [
          {
            "name": "get_current_weather",
            "description": "Get the current weather for a city",
            "input_schema": {
              "type": "object",
              "properties": {
                "city": {
                  "type": "string",
                  "description": "Name of the city, e.g. Berlin"
                },
                "unit": {
                  "type": "string",
                  "enum": [
                    "celsius",
                    "fahrenheit"
                  ],
                  "description": "Temperature unit"
                }
              },
              "required": [
                "city"
              ]
            }
          }
        ],
        "messages": [
          {
            "role": "user",
            "content": "Do I need an umbrella in Berlin today?"
          }
        ]
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

        test("tool_use block", function () {
          const b = res.getBody();
          expect(b.stop_reason).to.equal("tool_use");
          const block = b.content.find(c => c.type === "tool_use");
          expect(block).to.have.property("id");
          expect(block.input).to.be.an("object");
          bru.setVar("anthropicToolUseId", block.id);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: |-
  # Messages (Tools)

  Tools in Anthropic format with `input_schema`. The response contains a `tool_use` block:

  ```json
  { "type": "tool_use", "id": "toolu_...", "name": "get_current_weather", "input": { "city": "Berlin" } }
  ```

  The `id` is stored in `anthropicToolUseId` -> continue with **Messages (Tool Result)**.
