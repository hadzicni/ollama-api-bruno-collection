info:
  name: Messages (Multi-Turn)
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/v1/messages"
  body:
    type: json
    data: |-
      {
        "model": "{{model}}",
        "max_tokens": 512,
        "messages": [
          {
            "role": "user",
            "content": "My favorite fruit is mango."
          },
          {
            "role": "assistant",
            "content": "Great choice! Mangoes are sweet and rich in vitamin C."
          },
          {
            "role": "user",
            "content": "Which recipe would you recommend with my favorite fruit?"
          }
        ]
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

        test("Context was taken into account", function () {
          expect(res.getBody().content[0].text.toLowerCase()).to.contain("mango");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
