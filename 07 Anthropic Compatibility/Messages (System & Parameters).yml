info:
  name: Messages (System & Parameters)
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/v1/messages"
  body:
    type: json
    data: |-
      {
        "model": "{{model}}",
        "max_tokens": 512,
        "system": "You are a sommelier. Answer elegantly and in no more than 3 sentences.",
        "messages": [
          {
            "role": "user",
            "content": "Which wine goes well with mac and cheese?"
          }
        ],
        "temperature": 0.8,
        "top_p": 0.9,
        "top_k": 40,
        "stop_sequences": [
          "\n\n\n"
        ]
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

        test("Text response", function () {
          expect(res.getBody().content[0].text).to.not.be.empty;
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
