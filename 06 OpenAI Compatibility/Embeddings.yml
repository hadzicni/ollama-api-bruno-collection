info:
  name: Embeddings
  type: http
  seq: 10

http:
  method: POST
  url: "{{baseUrl}}/v1/embeddings"
  body:
    type: json
    data: |-
      {
        "model": "{{embedModel}}",
        "input": [
          "First text",
          "Second text"
        ],
        "encoding_format": "float",
        "dimensions": 512
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

        test("Two embeddings in OpenAI format", function () {
          const b = res.getBody();
          expect(b.object).to.equal("list");
          expect(b.data).to.have.lengthOf(2);
          expect(b.data[0].object).to.equal("embedding");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: |-
  # Embeddings

  `encoding_format`: `"float"` or `"base64"`. `dimensions` only for supported models.
