info:
  name: Models
  type: folder
  seq: 5

request:
  auth: inherit

docs:
  content: |-
    # Models

    Manage local models: list, show details, pull, copy, create, push and delete.

    Note: **Delete Model** only deletes `{{copyModelName}}` by default - the copy created by *Copy Model*.
  type: text/markdown
